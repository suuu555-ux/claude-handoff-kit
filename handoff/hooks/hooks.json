{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,os; p=os.path.expanduser('~/.claude/settings.json'); s=json.load(open(p)) if os.path.exists(p) else {}; c=(s.get('statusLine') or {}).get('command',''); print('[handoff] 狀態列尚未設定：跑一次 /handoff:setup 就能開啟 40% 收工提醒。') if not c else None\""
          }
        ]
      }
    ]
  }
}
